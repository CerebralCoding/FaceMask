name: Build and Release FaceMask

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g. v1.0.0)'
        required: true
        type: string

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install notcurses
        run: |
          brew update
          brew install notcurses

      - name: Install Swift 6.1.0
        run: |
          curl -L https://github.com/swiftlang/swift/releases/download/6.1.0/swift-6.1.0-macos.tar.gz -o swift.tar.gz
          tar -xzf swift.tar.gz
          echo "$PWD/swift-6.1.0-macos/usr/bin" >> $GITHUB_PATH

      - name: Build
        run: |
          swift build -c release

      - name: Package Binary
        run: |
          mkdir -p dist
          cp .build/release/FaceMask dist/FaceMask
          chmod +x dist/FaceMask

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.version }}
          name: Release ${{ inputs.version }}
          files: dist/FaceMask
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
